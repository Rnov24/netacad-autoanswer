# Implementation Plans — NetAcad AutoAnswer

Generated by the `/improve` skill on 2026-07-21 against commit `ba71b0f`.
Plans 008–011 added on 2026-07-23 against commit `d395c7c` — focused on Auto-Scroll & Course Completion improvements.

Execute in the order below. Each executor: read the plan fully before starting,
honor its STOP conditions, and update the Status column when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| [001](./001-fix-scrape-lock-and-retry.md) | Fix isScrapeInFlight race & unawaited retry | P1 | S | — | DONE |
| [002](./002-fix-xss-via-innerhtml.md) | Eliminate XSS via innerHTML in UI panels | P1 | S | — | DONE |
| [003](./003-decouple-stop-flags.md) | Decouple Quiz stop flag from Course Scroller | P1 | S | — | DONE |
| [004](./004-lru-cache-for-api-answers.md) | Add LRU eviction to apiAnswerCache | P2 | S | 001 | DONE |
| [005](./005-update-readme-and-agents-md.md) | Update README + create AGENTS.md | P2 | S | — | DONE |
| [006](./006-fix-object-matching-question-pipeline.md) | Fix Object-Matching / Connecting question pipeline | P1 | M | — | DONE |
| [007](./007-sequential-quiz-auto-pilot-navigation.md) | Enforce sequential question navigation and active-tab awareness in Quiz Auto-Pilot | P1 | M | 001, 003 | DONE |
| [008](./008-fix-course-scroller-await-and-double-execution.md) | Fix unawaited recordPageData & eliminate double video/check-button sweep | P1 | S | — | DONE |
| [009](./009-fix-scroll-timing-aria-toc-logging.md) | Fix scroll timing, scope aria-expanded expansion to ToC sidebar, suppress ToC log spam | P1 | S | 008 | DONE |
| [010](./010-course-scroller-pause-and-resilience.md) | Add pause/resume support and per-section error resilience to Course Scroller | P1 | S | — | DONE |
| [011](./011-scroller-multi-question-and-progress.md) | Handle multi-question CYU pages and add scroller progress reporting | P2 | M | 008, 010 | DONE |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED (reason)` | `REJECTED (reason)`

## Dependency notes

- 004 (LRU cache) is best executed after 001 (scrape lock fix) so the cache
  behavior is tested against the corrected lock semantics.
- 001, 002, 003, 005 are all independent and can be executed in parallel.
- **009 depends on 008**: plan 009 assumes the duplicate sweep calls have already
  been removed from `autoScrollModulePage` by plan 008, otherwise the scroll function
  excerpt in plan 009 won't match the live file.
- **011 depends on 008 and 010**: uses `await recordPageData()` (008) and
  `waitMsScroller` (010) in the multi-question loop; if either is absent, follow
  the STOP/fallback instructions in plan 011.
- 008 and 010 are independent and can be executed in parallel.

## Findings considered and rejected / deferred

| Finding | Decision |
|---|---|
| DEBT-04: Replace window/globalThis global pollution with namespace | DEFERRED — MV3 content scripts cannot use ES modules; the refactor requires a build step (e.g. esbuild) which is a larger architectural change than a single plan warrants. Worth doing, but blocked on agreeing on a bundler. |
| DEBT-02: Extract shared resolveFn | DEFERRED — blocked on DEBT-04; no clean home for a shared utility without a module system. |
| DX-05: Add web-ext dev runner | DEFERRED — no npm/package.json exists; adding one is a pre-req; low urgency. |
| DX-08: Add ESLint + Prettier | DEFERRED — depends on having a package.json (DX-05 pre-req). |
| BUG-04: Log swallowed expand-button errors | NOT WORTH PLANNING — one-liner; executor can add console.debug directly. |
| BUG-07: Consolidate double window/globalThis export | MERGED INTO DEBT-04 — same root cause. |
| DX-04: Standardize extension naming | LOW URGENCY — cosmetic; not worth a full plan; can be done as part of 005. |
| PERF-01 (Shadow root cache): Pass cached roots to all per-iteration functions | DEFERRED — requires changing all 5 function signatures simultaneously (getShadowRoots callers); larger refactor than the performance gain justifies for the scroller. Revisit if profiling shows DOM traversal as bottleneck. |
| ARCH-02 (Unified automation state): Refactor runner state into AutomationState module | DEFERRED — larger architectural change blocked on DEBT-04 (no module system). The pause-reuse in plan 010 is the pragmatic alternative. |
| BUG-05 (Fraction regex false-positive in isTocItemCompleted): Restrict regex to progress indicators | INVESTIGATE — MED confidence only; depends on NetAcad's actual ToC item text format. Not planned until confirmed false-positive on a live course. |
| PERF-04 (Adaptive DOM polling): Replace fixed 1200ms sleep with DOM readiness polling | DEFERRED — complexity outweighs benefit for now; plan 010 replaces the bare sleep with waitMsScroller which at least respects pause/stop. |
| DIR-03 (Mute video before play + full event sequence): Set video.muted=true before autoplay | NOT WORTH PLANNING — scraper already catches autoplay errors with .catch(()=>{}); seeking to duration-0.2 is sufficient for progress tracking regardless of play() success. |
| ARCH-06 (Completion notification via chrome.notifications): Show desktop notification when course done | DEFERRED — requires adding `notifications` permission to manifest.json; plan 011's progress banner covers the immediate UX need. |

## Audit coverage

**2026-07-21 audit** (plans 001–007): `api.js`, `ui.js`, `scraper.js`, `content.js`, `background.js`,
`popup.js`, `popup.html`, `manifest.json`, `README.md`, `CONTRIBUTING.md`

**2026-07-23 focused audit** (plans 008–011): `scraper.js` (runCourseScrollerLoop + recordPageData),
`ui.js` (autoScrollModulePage, parseThreeLevelCourseToC, navigateToFirstIncompleteLevel3Item,
isTocItemCompleted, autoCompleteVideosOnPage, autoClickAllCheckButtonsOnPage, autoSubmitQuestion),
`content.js` (getStatus handler, running banner, message listener)

Not audited: `images/` (binary assets), git history beyond -30 commits

